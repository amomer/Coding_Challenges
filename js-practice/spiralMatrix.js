/**
 * @param {number[][]} matrix
 * @return {number[]}
 */
var spiralOrder = function(matrix) {
    var ret = [];
    m = matrix.length;
    n = matrix[0].length;
    while(ret.length < (m * n)) {
        for(let i = 0; i < )
    }
};